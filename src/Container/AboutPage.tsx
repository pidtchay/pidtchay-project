import React from 'react';

export const AboutPage = () => {
    return(
        <div>
            <h1>About</h1>
            <div>added this to verify that the task of publishing the project after my changes will work!</div>
        </div>
    );
};
